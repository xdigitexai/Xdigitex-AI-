# Prisma persistence

Inspect the schema and actual database state separately. Preserve the project root when reading `prisma/schema.prisma`. Verify migrations and call-record fields using non-destructive queries; schema presence is inspection evidence, not an end-to-end call result.
