# SSL acceptance

DNS binds the requested hostname to the owned target; the exact vhost and document root are identified; ACME routing works; a certificate is issued and bound; SAN matches; chain and SNI are correct; public HTTPS succeeds without insecure flags; renewal is configured.
